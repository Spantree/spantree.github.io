<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Spantree's Blog]]></title>
  <link href="http://Spantree.github.io/atom.xml" rel="self"/>
  <link href="http://Spantree.github.io/"/>
  <updated>2014-02-12T16:06:23-06:00</updated>
  <id>http://Spantree.github.io/</id>
  <author>
    <name><![CDATA[Spantree]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Backbone Repository Pattern]]></title>
    <link href="http://Spantree.github.io/blog/2014/02/12/backbone-repository-pattern/"/>
    <updated>2014-02-12T22:10:00-06:00</updated>
    <id>http://Spantree.github.io/blog/2014/02/12/backbone-repository-pattern</id>
    <content type="html"><![CDATA[<p>&mdash;</p>

<h3>Stubbing Backbone&rsquo;s fetch()</h3>

<p>When we need to test a part of our application that fetches data
from a server, we will want to avoid making that request so as
not to slow down our tests; and because that endpoint might not
be implemented yet. Besides, we are focusing on unit tests, and they
should only test individual indpendent units. If our test makes an external
request, then it stops being a unit test and starts to venture into
the realm of integration or functional tests.</p>

<p>Let&rsquo;s consider the following code:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="k">class</span> <span class="nx">RateCalculator</span>
</span><span class='line'>  <span class="nv">calculate: </span><span class="nf">(ratePerHour) -&gt;</span>
</span><span class='line'>    <span class="nv">timeEntries = </span><span class="k">new</span> <span class="nx">TimeEntries</span><span class="p">()</span>
</span><span class='line'>    <span class="nx">timeEntries</span><span class="p">.</span><span class="nx">fetch</span><span class="p">()</span>
</span><span class='line'>    <span class="nx">_</span><span class="p">.</span><span class="nx">reduce</span><span class="p">(</span><span class="nx">timeEntries</span><span class="p">.</span><span class="nx">models</span><span class="p">,</span> <span class="nf">(acc, timeEntry)-&gt;</span> <span class="nx">acc</span> <span class="o">*</span> <span class="nb">parseInt</span><span class="p">(</span><span class="nx">timeEntry</span><span class="p">.</span><span class="nx">hrs</span><span class="p">,</span> <span class="mi">10</span><span class="p">),</span> <span class="mi">20</span><span class="p">)</span>
</span></code></pre></td></tr></table></div></figure>


<p>This method calculates the total rate based on how many hours worked and the hourly rate. The hours is fetched via
an XHR request. Testing this can be a little challenging, but not impossible. We can use sinon to stub
the collection&rsquo;s <code>fetch</code> function:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="nx">describe</span> <span class="s">&#39;Rate Calculator&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>  <span class="nx">it</span> <span class="s">&#39;calculates the hourly rate&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>    <span class="nv">calculator = </span><span class="k">new</span> <span class="nx">RateCalculator</span><span class="p">()</span>
</span><span class='line'>    <span class="nv">timeEntriesStub = </span><span class="nx">sinon</span><span class="p">.</span><span class="nx">stub</span><span class="p">(</span><span class="nx">TimeEntries</span><span class="o">::</span><span class="p">,</span> <span class="s">&#39;fetch&#39;</span><span class="p">).</span><span class="nx">returns</span><span class="p">(</span><span class="k">new</span> <span class="nx">TimeEntries</span><span class="p">([{</span><span class="nv">hrs: </span><span class="mi">2</span><span class="p">},</span> <span class="p">{</span><span class="nv">hrs: </span><span class="mi">3</span><span class="p">}]))</span>
</span><span class='line'>    <span class="nx">expect</span><span class="p">(</span> <span class="nx">calculator</span><span class="p">.</span><span class="nx">calculate</span><span class="p">(</span><span class="mi">20</span><span class="p">)</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="mi">100</span>
</span><span class='line'>    <span class="nx">timeEntriesStub</span><span class="p">.</span><span class="nx">restore</span><span class="p">()</span>
</span></code></pre></td></tr></table></div></figure>


<p>While this will work, there are a couple things that should bother us. First, the RateCalculator
is tightly coupled to TimeEntries collection; but most importantly, the RateCalculator is doing too much.
It is retrieving the data AND calculating the rate. We can address this by &lsquo;injecting&rsquo; the TimeEntries
into the calculate method:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="k">class</span> <span class="nx">RateCalculator</span>
</span><span class='line'>  <span class="nv">calculate: </span><span class="nf">(ratePerHour, timeEntries) -&gt;</span>
</span><span class='line'>    <span class="nx">_</span><span class="p">.</span><span class="nx">reduce</span><span class="p">(</span><span class="nx">timeEntries</span><span class="p">.</span><span class="nx">models</span><span class="p">,</span> <span class="nf">(acc, timeEntry)-&gt;</span> <span class="nx">acc</span> <span class="o">*</span> <span class="nb">parseInt</span><span class="p">(</span><span class="nx">timeEntry</span><span class="p">.</span><span class="nx">hrs</span><span class="p">,</span> <span class="mi">10</span><span class="p">),</span> <span class="mi">20</span><span class="p">)</span>
</span></code></pre></td></tr></table></div></figure>


<p>This looks much simpler. Even our test looks better.</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="nx">describe</span> <span class="s">&#39;Rate Calculator&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>  <span class="nx">it</span> <span class="s">&#39;calculates the hourly rate&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>    <span class="nv">calculator = </span><span class="k">new</span> <span class="nx">RateCalculator</span><span class="p">()</span>
</span><span class='line'>    <span class="nv">timeEntriesStub = </span><span class="k">new</span> <span class="nx">TimeEntries</span><span class="p">([{</span><span class="nv">hrs: </span><span class="mi">2</span><span class="p">},</span> <span class="p">{</span><span class="nv">hrs: </span><span class="mi">3</span><span class="p">}])</span>
</span><span class='line'>    <span class="nx">expect</span><span class="p">(</span> <span class="nx">calculator</span><span class="p">.</span><span class="nx">calculate</span><span class="p">(</span><span class="mi">20</span><span class="p">)</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="mi">100</span>
</span></code></pre></td></tr></table></div></figure>


<p>If we step back for a moment, we can see that we might stumble on the same problem again. Consider
 testing some part of our application that uses RateCalculator. We might have some code similar to this:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="k">class</span> <span class="nx">RateController</span>
</span><span class='line'>  <span class="nv">onSubmittButton: </span><span class="nf">-&gt;</span>
</span><span class='line'>    <span class="nv">timeEntries = </span><span class="k">new</span> <span class="nx">TimeEntries</span><span class="p">().</span><span class="nx">fetch</span><span class="p">()</span>
</span><span class='line'>    <span class="nv">rateCalculator = </span><span class="k">new</span> <span class="nx">RateCalculator</span><span class="p">()</span>
</span><span class='line'>    <span class="nx">rateCalculator</span><span class="p">.</span><span class="nx">calculate</span><span class="p">(</span><span class="mi">20</span><span class="p">,</span> <span class="nx">timeEntries</span><span class="p">)</span>
</span></code></pre></td></tr></table></div></figure>


<p>When we try to test this we might have a situation similar to our first test:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="nx">describe</span> <span class="s">&#39;Rate Controller&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>  <span class="nx">it</span> <span class="s">&#39;calculates the hourly rate&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>    <span class="nv">rateController = </span><span class="k">new</span> <span class="nx">RateController</span><span class="p">()</span>
</span><span class='line'>    <span class="nv">timeEntriesStub = </span><span class="nx">sinon</span><span class="p">.</span><span class="nx">stub</span><span class="p">(</span><span class="nx">TimeEntries</span><span class="o">::</span><span class="p">,</span> <span class="s">&#39;fetch&#39;</span><span class="p">).</span><span class="nx">returns</span><span class="p">(</span><span class="k">new</span> <span class="nx">TimeEntries</span><span class="p">([{</span><span class="nv">hrs: </span><span class="mi">2</span><span class="p">},</span> <span class="p">{</span><span class="nv">hrs: </span><span class="mi">3</span><span class="p">}]))</span>
</span><span class='line'>    <span class="nx">expect</span><span class="p">(</span> <span class="nx">rateCalculator</span><span class="p">.</span><span class="nx">onSubmitButton</span><span class="p">()</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="mi">100</span>
</span><span class='line'>    <span class="nx">timeEntriesStub</span><span class="p">.</span><span class="nx">restore</span><span class="p">()</span>
</span></code></pre></td></tr></table></div></figure>


<p>If we need to use RateCalculator in other parts of our application, we will duplicate this in many places
and lose track of it. Why is this bad again? Because it couples our application to Backbone and creates
some unmaintainable code. It will be difficult to change this if we ever decide that maybe Backbone is not
the right choice, or if our TimeEntries is a more complex collection</p>

<p>We can solve this by injecting the collection, like we did in the first scenario, and it is the route
I would take; but we will reach a point where passing the collection like a hot potato will not save us,
and we will have to deal with this situation head on.</p>

<p>And what about situations where we would want to trigger an action when a new set of collections is fetched?
Or maybe we have a real-time dashboard that needs to get updated on near real time every time some event
happens somewhere else (even on a remote computer or server)? How do we handle error messages for all xhr requests?</p>

<p>What about scenarios where the xhr response does not fit nicely with our collections and entities? For example,
suppose we need to request data about &lsquo;things to do&rsquo; and the structure of that data returned is something like:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
<span class='line-number'>15</span>
<span class='line-number'>16</span>
<span class='line-number'>17</span>
</pre></td><td class='code'><pre><code class='json'><span class='line'><span class="p">{</span>
</span><span class='line'><span class="err">venues:</span> <span class="err">[{name:</span> <span class="err">&#39;Venue</span> <span class="err">A&#39;,</span> <span class="err">qty:</span> <span class="err">10</span><span class="p">}</span><span class="err">,</span>
</span><span class='line'>        <span class="p">{</span><span class="err">name:</span> <span class="err">&#39;Venue</span> <span class="err">B&#39;,</span> <span class="err">qty:</span> <span class="err">65</span><span class="p">}</span><span class="err">,</span>
</span><span class='line'>        <span class="p">{</span><span class="err">name:</span> <span class="err">&#39;Venue</span> <span class="err">C&#39;,</span> <span class="err">qty:</span> <span class="err">31</span><span class="p">}</span>
</span><span class='line'>        <span class="err">],</span>
</span><span class='line'><span class="err">dates:</span> <span class="p">[{</span><span class="err">when:</span> <span class="err">&#39;Today&#39;,</span> <span class="err">qty:</span> <span class="err">7</span><span class="p">},</span>
</span><span class='line'>        <span class="p">{</span><span class="err">when:</span> <span class="err">&#39;Tomorrow&#39;,</span> <span class="err">qty:</span> <span class="err">13</span><span class="p">},</span>
</span><span class='line'>        <span class="p">{</span><span class="err">when:</span> <span class="err">&#39;Friday&#39;,</span> <span class="err">qty:</span> <span class="err">41</span><span class="p">}</span>
</span><span class='line'>      <span class="p">]</span>
</span><span class='line'><span class="err">thingsToDo:</span> <span class="p">[{</span><span class="err">name:</span> <span class="err">&#39;Concert</span> <span class="err">A&#39;,</span> <span class="err">date:</span> <span class="err">&#39;2014-01-31</span> <span class="err">20:00&#39;</span><span class="p">},</span>
</span><span class='line'>            <span class="p">{</span><span class="err">name:</span> <span class="err">&#39;Comedy</span> <span class="err">Club&#39;,</span> <span class="err">date:</span> <span class="err">&#39;2014-01-31</span> <span class="err">19:00&#39;</span><span class="p">}</span>
</span><span class='line'>            <span class="err">......</span>
</span><span class='line'>            <span class="p">]</span>
</span><span class='line'><span class="err">rel:</span><span class="p">[{</span><span class="err">currentPage:</span> <span class="err">&#39;http://some.domain/2/20&#39;</span><span class="p">},</span>
</span><span class='line'>      <span class="p">{</span><span class="err">nextPage:</span> <span class="err">&#39;http://some.domain/3/20&#39;</span><span class="p">},</span>
</span><span class='line'>      <span class="p">{</span><span class="err">previousPage:</span> <span class="err">&#39;http://some.domain/1/20&#39;</span><span class="p">}</span>
</span><span class='line'>  <span class="p">]</span>
</span></code></pre></td></tr></table></div></figure>


<h3>Repository Pattern.</h3>

<p>If we step back for a bit, and imagine our application as a vulnerable kernel that needs to be shielded from any
outside infection, then we can see how allowing direct calls to Backbone.Collection#fetch() deep in our application
exposes it to external factors. Changes to the Backbone API can require massive changes deep in our application&rsquo;s
kernel.</p>

<p>We can take some inspiration from the <a href="http://alistair.cockburn.us/Hexagonal+architecture">&lsquo;Hexagonal Architecture&rsquo;</a> to help us come up with a &lsquo;cleaner&rsquo; solution.
In order to access any external system (like xhr requests), we need to add an interface for it.</p>

<p>We might want to start by defining the interface:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
<span class='line-number'>15</span>
<span class='line-number'>16</span>
<span class='line-number'>17</span>
<span class='line-number'>18</span>
<span class='line-number'>19</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="nx">describe</span> <span class="s">&#39;Time Entry Repository&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>  <span class="nx">describe</span> <span class="s">&#39;Configure the end points&#39;</span>
</span><span class='line'>    <span class="nx">it</span> <span class="s">&#39;When end points are not uniform&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>      <span class="nv">repository = </span><span class="k">new</span> <span class="nx">TimeEntryRepository</span>
</span><span class='line'>        <span class="nv">get: </span> <span class="s">&#39;...&#39;</span>
</span><span class='line'>        <span class="nv">post: </span><span class="s">&#39;...&#39;</span>
</span><span class='line'>        <span class="nv">put: </span> <span class="s">&#39;...&#39;</span>
</span><span class='line'>        <span class="nv">list: </span><span class="s">&#39;...&#39;</span>
</span><span class='line'>      <span class="nx">expect</span><span class="p">(</span> <span class="nx">repository</span><span class="p">.</span><span class="nx">getURL</span><span class="p">()</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="s">&#39;...&#39;</span>
</span><span class='line'>      <span class="nx">expect</span><span class="p">(</span> <span class="nx">repository</span><span class="p">.</span><span class="nx">postURL</span><span class="p">()</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="s">&#39;...&#39;</span>
</span><span class='line'>      <span class="nx">expect</span><span class="p">(</span> <span class="nx">repository</span><span class="p">.</span><span class="nx">putURL</span><span class="p">()</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="s">&#39;...&#39;</span>
</span><span class='line'>      <span class="nx">expect</span><span class="p">(</span> <span class="nx">repository</span><span class="p">.</span><span class="nx">listURL</span><span class="p">()</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="s">&#39;...&#39;</span>
</span><span class='line'>    <span class="nx">it</span> <span class="s">&#39;When end points are uniform&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>      <span class="nv">repository = </span><span class="k">new</span> <span class="nx">TimeEntryRepository</span>
</span><span class='line'>        <span class="nv">all: </span> <span class="s">&#39;...&#39;</span>
</span><span class='line'>      <span class="nx">expect</span><span class="p">(</span> <span class="nx">repository</span><span class="p">.</span><span class="nx">getURL</span><span class="p">()</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="s">&#39;...&#39;</span>
</span><span class='line'>      <span class="nx">expect</span><span class="p">(</span> <span class="nx">repository</span><span class="p">.</span><span class="nx">postURL</span><span class="p">()</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="s">&#39;...&#39;</span>
</span><span class='line'>      <span class="nx">expect</span><span class="p">(</span> <span class="nx">repository</span><span class="p">.</span><span class="nx">putURL</span><span class="p">()</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="s">&#39;...&#39;</span>
</span><span class='line'>      <span class="nx">expect</span><span class="p">(</span> <span class="nx">repository</span><span class="p">.</span><span class="nx">listURL</span><span class="p">()</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="s">&#39;...&#39;</span>
</span></code></pre></td></tr></table></div></figure>


<p>Our repository makes provisions for cases where the url does not fit nicely with Backbone&rsquo;s
expectations. Backbone expects all urls to be the same and the only thing that changes
is the http verb. There might be cases where that is not possible.</p>

<h4>Fetch API</h4>

<p>Now we can specify how we will fetch data from the server:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="nx">describe</span> <span class="s">&#39;Time Entry Repository&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>  <span class="nx">describe</span> <span class="s">&#39;Fetch API&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>    <span class="nx">describe</span> <span class="s">&#39;When it fetches data from remote server&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>      <span class="nx">it</span> <span class="s">&#39;then makes an xhr request&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>        <span class="nv">xhrMock = </span><span class="nx">sinon</span><span class="p">.</span><span class="nx">mock</span><span class="p">(</span><span class="nx">$</span><span class="p">,</span> <span class="s">&#39;ajax&#39;</span><span class="p">).</span><span class="nx">returns</span><span class="p">({})</span>
</span><span class='line'>        <span class="nv">repository = </span><span class="k">new</span> <span class="nx">TimeEntryRepository</span><span class="p">()</span>
</span><span class='line'>        <span class="nx">repository</span><span class="p">.</span><span class="nx">all</span><span class="p">(</span><span class="s">&#39;http://a.url/timeEntries&#39;</span><span class="p">)</span>
</span><span class='line'>        <span class="nx">expect</span><span class="p">(</span><span class="nx">xhr</span><span class="p">.</span><span class="nx">calledOnce</span><span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="kc">true</span>
</span><span class='line'>        <span class="nx">xhrMock</span><span class="p">.</span><span class="nx">restore</span><span class="p">()</span>
</span></code></pre></td></tr></table></div></figure>


<p>This test only needs to ensure that an xhr request is made with jquery. It is testing the
repository&rsquo;s internal implementation, which consequently makes it very fragile. However, an object like
this lives in the periphery of an application and for them these kinds of tests are common. We will later
see how it makes our domain logic much cleaner. In the meantime, we can make this test pass:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="k">class</span> <span class="nx">TimeRepository</span>
</span><span class='line'>  <span class="nv">all: </span><span class="nf">(url) -&gt;</span>
</span><span class='line'>    <span class="vi">@_listUrl = </span><span class="nx">url</span>
</span><span class='line'>    <span class="vi">@_getUrl = </span><span class="nx">url</span>
</span><span class='line'>    <span class="p">...</span>
</span><span class='line'>
</span><span class='line'>  <span class="nv">listUrl: </span><span class="nf">-&gt;</span>
</span><span class='line'>    <span class="nx">@_listUrl</span>
</span><span class='line'>
</span><span class='line'>  <span class="nv">fetch: </span><span class="nf">-&gt;</span>
</span><span class='line'>    <span class="nx">$</span><span class="p">.</span><span class="nx">ajax</span><span class="p">(</span><span class="nv">url: </span><span class="nx">@listUrl</span><span class="p">())</span>
</span></code></pre></td></tr></table></div></figure>


<p>Our fetch function could have also used a backbone collection. The clients using the repository won&rsquo;t know
nor need to know how it is implemented. But this still looks a little wonky. We haven&rsquo;t gotten anything benefits
from it&hellip; yet.</p>

<p>Now lets consider a scenario where we&rsquo;d like to <code>promisify</code> our interface:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="nx">describe</span> <span class="s">&#39;When it fetches data from remote server&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>  <span class="nx">it</span> <span class="s">&#39;then it returns a promise&#39;</span><span class="p">,</span> <span class="nf">(done)-&gt;</span>
</span><span class='line'>    <span class="nv">xhrMock = </span><span class="nx">sinon</span><span class="p">.</span><span class="nx">stub</span><span class="p">(</span><span class="nx">$</span><span class="p">,</span> <span class="s">&#39;ajax&#39;</span><span class="p">).</span><span class="nx">returns</span><span class="p">([</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">4</span><span class="p">,</span><span class="mi">5</span><span class="p">])</span>
</span><span class='line'>    <span class="nv">repository = </span><span class="k">new</span> <span class="nx">TimeEntryRepository</span><span class="p">()</span>
</span><span class='line'>    <span class="nx">repository</span>
</span><span class='line'>      <span class="p">.</span><span class="nx">all</span><span class="p">(</span><span class="s">&#39;http://a.url/timeEntries&#39;</span><span class="p">)</span>
</span><span class='line'>      <span class="k">then</span><span class="nf">(timeEntries) -&gt;</span>
</span><span class='line'>        <span class="nx">expect</span><span class="p">(</span><span class="nx">timeEntries</span><span class="p">.</span><span class="nx">length</span><span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="mi">5</span>
</span><span class='line'>        <span class="nx">done</span><span class="p">()</span>
</span><span class='line'>    <span class="nx">xhrMock</span><span class="p">.</span><span class="nx">restore</span><span class="p">()</span>
</span></code></pre></td></tr></table></div></figure>


<p>This is a contrived example, but bear with me for a while. We return an array of 5 items and we need to tell
mocha that this is an async test. We do this by passing <code>done</code> as an argument to the test and then invoking
<code>done()</code> to notify mocha that the async test has finished. We can then make assertions on the value of the
promise. Our specs now start to look more like production code. The only inconvenience at the moment is
creating some fake data. But we can live with that. Much better than coupling all our tests (and production
code to jquery). And we don&rsquo;t need to change anything in our code to make this pass because $.ajax() returns
a promise.</p>

<p>This is okay for very simple cases, but what if we need to return something with behavior (an instance of an object):</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="nx">describe</span> <span class="s">&#39;When it fetches data from remote server&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>  <span class="nx">it</span> <span class="s">&#39;then it returns a promise&#39;</span><span class="p">,</span> <span class="nf">(done)-&gt;</span>
</span><span class='line'>    <span class="nv">xhrMock = </span><span class="nx">sinon</span><span class="p">.</span><span class="nx">stub</span><span class="p">(</span><span class="nx">$</span><span class="p">,</span> <span class="s">&#39;ajax&#39;</span><span class="p">).</span><span class="nx">returns</span><span class="p">([</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">4</span><span class="p">,</span><span class="mi">5</span><span class="p">])</span>
</span><span class='line'>    <span class="nv">repository = </span><span class="k">new</span> <span class="nx">TimeEntryRepository</span><span class="p">()</span>
</span><span class='line'>    <span class="nx">repository</span>
</span><span class='line'>      <span class="p">.</span><span class="nx">all</span><span class="p">(</span><span class="s">&#39;http://a.url/timeEntries&#39;</span><span class="p">)</span>
</span><span class='line'>      <span class="k">then</span><span class="nf">(timeEntries) -&gt;</span>
</span><span class='line'>        <span class="nx">expect</span><span class="p">(</span><span class="nx">timeEntries</span> <span class="k">instanceof</span> <span class="nx">TimeEntries</span><span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="kc">true</span>
</span><span class='line'>        <span class="nx">done</span><span class="p">()</span>
</span><span class='line'>    <span class="nx">xhrMock</span><span class="p">.</span><span class="nx">restore</span><span class="p">()</span>
</span></code></pre></td></tr></table></div></figure>


<p>Implementing this wouldn&rsquo;t be so difficult:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="nv">fetch: </span><span class="nf">-&gt;</span>
</span><span class='line'>  <span class="nv">promise = </span><span class="nx">Q</span><span class="p">.</span><span class="nx">when</span><span class="p">(</span><span class="nx">$</span><span class="p">.</span><span class="nx">ajax</span><span class="p">(</span><span class="nv">url: </span><span class="nx">@listUrl</span><span class="p">()))</span>
</span><span class='line'>  <span class="nx">promise</span><span class="p">.</span><span class="nx">then</span> <span class="nf">(response) =&gt;</span>
</span><span class='line'>    <span class="k">new</span> <span class="nx">TimeEntries</span><span class="p">(</span><span class="nx">response</span><span class="p">)</span>
</span></code></pre></td></tr></table></div></figure>


<p>We introduced the Q library for dealing with promises (a matter of personal preference). Our clients don&rsquo;t need
to know what library is being used, they just need to know its a Promises/A+ compatible library. So we should be
able to substitute the Q library for Bluebird if we need to and any client of this class won&rsquo;t need to change.</p>

<h4>Notifications</h4>

<p>We can now even start introducing custom events that our application can listen to. An event for fetching data, or
for creating a new entity, deleting one or updating one; can be triggered for any part of our application to
listen to:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
<span class='line-number'>15</span>
<span class='line-number'>16</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="nx">describe</span> <span class="s">&#39;Time Entry Repository&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>  <span class="nx">beforeEach</span> <span class="nf">-&gt;</span>
</span><span class='line'>    <span class="vi">@caughtEvent = </span><span class="kc">false</span>
</span><span class='line'>    <span class="vi">@aRemoteObject =</span>
</span><span class='line'>      <span class="nv">listen: </span><span class="nf">(repository) =&gt;</span>
</span><span class='line'>        <span class="nx">repository</span><span class="p">.</span><span class="nx">onFetch</span> <span class="o">=&gt;</span>
</span><span class='line'>          <span class="vi">@caughtEvent = </span><span class="kc">true</span>
</span><span class='line'>
</span><span class='line'>  <span class="nx">it</span> <span class="s">&#39;Trigger an event when entries are fetched&#39;</span><span class="p">,</span> <span class="nf">(done)-&gt;</span>
</span><span class='line'>    <span class="nv">repository = </span><span class="k">new</span> <span class="nx">TimeEntryRepository</span><span class="p">()</span>
</span><span class='line'>    <span class="nx">repository</span><span class="p">.</span><span class="nx">all</span><span class="p">(</span><span class="s">&#39;http://a.url/timeEntries&#39;</span><span class="p">)</span>
</span><span class='line'>    <span class="nv">xhrStub = </span><span class="nx">sinon</span><span class="p">.</span><span class="nx">stub</span><span class="p">(</span><span class="nx">$</span><span class="p">,</span> <span class="s">&#39;ajax&#39;</span><span class="p">).</span><span class="nx">returns</span><span class="p">([</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">4</span><span class="p">,</span><span class="mi">5</span><span class="p">])</span>
</span><span class='line'>    <span class="nx">@aRemoteObject</span><span class="p">.</span><span class="nx">listen</span><span class="p">(</span><span class="nx">repository</span><span class="p">)</span>
</span><span class='line'>    <span class="nx">repository</span><span class="p">.</span><span class="nx">fetch</span><span class="p">()</span>
</span><span class='line'>    <span class="nx">expect</span><span class="p">(</span> <span class="nx">@caughtEvent</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="kc">true</span>
</span><span class='line'>    <span class="nx">xhrStub</span><span class="p">.</span><span class="nx">restore</span><span class="p">()</span>
</span></code></pre></td></tr></table></div></figure>




<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="k">class</span> <span class="nx">TimeEntryRepository</span>
</span><span class='line'>  <span class="nv">constructor: </span><span class="nf">-&gt;</span>
</span><span class='line'>    <span class="vi">@vent = </span><span class="nx">_</span><span class="p">.</span><span class="nx">extend</span> <span class="p">{},</span> <span class="nx">Backbone</span><span class="p">.</span><span class="nx">Events</span>
</span><span class='line'>
</span><span class='line'>  <span class="nv">onFetch: </span><span class="nf">(callback) -&gt;</span>
</span><span class='line'>    <span class="nx">@vent</span><span class="p">.</span><span class="nx">on</span> <span class="s">&#39;fetched&#39;</span><span class="p">,</span> <span class="nx">callback</span>
</span><span class='line'>
</span><span class='line'>  <span class="nv">fetch: </span><span class="nf">-&gt;</span>
</span><span class='line'>    <span class="nv">promise = </span><span class="nx">Q</span><span class="p">.</span><span class="nx">when</span><span class="p">(</span><span class="nx">$</span><span class="p">.</span><span class="nx">ajax</span><span class="p">(</span><span class="nv">url: </span><span class="nx">@listUrl</span><span class="p">()))</span>
</span><span class='line'>    <span class="nx">promise</span><span class="p">.</span><span class="nx">then</span> <span class="nf">(response) =&gt;</span>
</span><span class='line'>      <span class="nv">timeEntries = </span><span class="k">new</span> <span class="nx">TimeEntries</span><span class="p">(</span><span class="nx">response</span><span class="p">)</span>
</span><span class='line'>      <span class="nx">@vent</span><span class="p">.</span><span class="nx">trigger</span> <span class="s">&#39;fetched&#39;</span><span class="p">,</span> <span class="nx">timeEntries</span>
</span></code></pre></td></tr></table></div></figure>


<p>Now any part of our application can listen directly for any events triggered by the repository and execute
a function accordingly. This enables us to update different parts of our UI independent of each other. We can
also test those parts our application pretty easily by just triggering the event without needing to wire up
the entire repository:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="nx">repository</span><span class="p">.</span><span class="nx">vent</span><span class="p">.</span><span class="nx">trigger</span> <span class="s">&#39;fetched&#39;</span>
</span></code></pre></td></tr></table></div></figure>


<p>In some cases, we might only need to create some fake time entries, but that is to be expected.</p>

<h4>Handling Network Failures</h4>

<p>This strategy can also enable us to handle network errors in a generic manner. If our app is a single
page app that is expected to be kept running on the browser for long periods of time (even days or weeks),
whenever the network is down or an xhr request failed for some other reason, we should be able to notify the user.
Although we can do this with Backbone by overriding <em>sync</em>, it feels like a hack and I am always wary of
overriding third party libraries.</p>

<p>To handle network failures for all xhr requests we would either need to implement that code in every repository,
or use inheritance (or mixins also). We&rsquo;ll use inheritance in this case since it is something only
repositories will need to do:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
<span class='line-number'>15</span>
<span class='line-number'>16</span>
<span class='line-number'>17</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="nx">describe</span> <span class="s">&#39;Time Entry Repository&#39;</span><span class="p">,</span> <span class="nf">-&gt;</span>
</span><span class='line'>  <span class="nx">beforeEach</span> <span class="nf">-&gt;</span>
</span><span class='line'>    <span class="vi">@handledError = </span><span class="kc">false</span>
</span><span class='line'>    <span class="vi">@errorHandlingFn = </span><span class="nf">-&gt;</span> <span class="vi">@handledError = </span><span class="kc">true</span>
</span><span class='line'>
</span><span class='line'>  <span class="nx">it</span> <span class="s">&#39;Handles XHR errors&#39;</span><span class="p">,</span> <span class="nf">(done)-&gt;</span>
</span><span class='line'>    <span class="nv">repository = </span><span class="k">new</span> <span class="nx">TimeEntryRepository</span><span class="p">()</span>
</span><span class='line'>    <span class="nx">repository</span><span class="p">.</span><span class="nx">all</span><span class="p">(</span><span class="s">&#39;http://a.url/timeEntries&#39;</span><span class="p">)</span>
</span><span class='line'>    <span class="nv">deferred = </span><span class="nx">Q</span><span class="p">.</span><span class="nx">defer</span><span class="p">()</span>
</span><span class='line'>    <span class="nv">xhrStub = </span><span class="nx">sinon</span><span class="p">.</span><span class="nx">stub</span><span class="p">(</span><span class="nx">$</span><span class="p">,</span> <span class="s">&#39;ajax&#39;</span><span class="p">).</span><span class="nx">returns</span><span class="p">(</span><span class="nx">deferred</span><span class="p">)</span>
</span><span class='line'>    <span class="nx">repository</span>
</span><span class='line'>      <span class="p">.</span><span class="nx">fetch</span><span class="p">()</span>
</span><span class='line'>      <span class="p">.</span><span class="nx">then</span><span class="p">(</span> <span class="nf">-&gt;</span> <span class="nx">console</span><span class="p">.</span><span class="nx">log</span> <span class="s">&#39;Success&#39;</span>
</span><span class='line'>      <span class="p">,</span> <span class="nx">@handleError</span><span class="p">)</span>
</span><span class='line'>    <span class="nx">deferred</span><span class="p">.</span><span class="nx">reject</span><span class="p">()</span>
</span><span class='line'>    <span class="nx">expect</span><span class="p">(</span> <span class="nx">@handledError</span> <span class="p">).</span><span class="nx">to</span><span class="p">.</span><span class="nx">be</span> <span class="kc">true</span>
</span><span class='line'>    <span class="nx">xhrStub</span><span class="p">.</span><span class="nx">restore</span><span class="p">()</span>
</span></code></pre></td></tr></table></div></figure>


<p>We can handle errors with promises by passing in a function as an error handler to the <em>then()</em> method.
We could also have an application event bus which we can notify of any errors from within this error handler:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
</pre></td><td class='code'><pre><code class='coffeescript'><span class='line'><span class="nx">repository</span>
</span><span class='line'>  <span class="p">.</span><span class="nx">fetch</span><span class="p">()</span>
</span><span class='line'>  <span class="p">.</span><span class="nx">then</span><span class="p">(</span><span class="kc">null</span><span class="p">,</span> <span class="nf">(error)-&gt;</span> <span class="nx">app</span><span class="p">.</span><span class="nx">eventBus</span><span class="p">.</span><span class="nx">trigger</span> <span class="s">&#39;network:error&#39;</span><span class="p">)</span>
</span><span class='line'>
</span><span class='line'><span class="p">...</span>
</span><span class='line'><span class="nx">app</span><span class="p">.</span><span class="nx">eventBus</span><span class="p">.</span><span class="nx">on</span>  <span class="s">&#39;network:error&#39;</span><span class="p">,</span> <span class="nx">showErrorMessage</span>
</span></code></pre></td></tr></table></div></figure>


<h3>Conclusion</h3>

<p>We can use the Repository Pattern to decouple our application from any framework specific code that can
make our application hard to test. As a secondary benefit, our objects become more focused by having only
one primary reason to change if all they do is just concern themselves with fetching and sending data.
Promises can also be used to make our code more readable when dealing with async operations. They also help
us deal with error handling in an elegant manner. Finally, the use of a messaging pattern and an event bus
helps us keep the components of our application decoupled by enabling us to send messages to remote objects
without having to hold a direct reference to them. We can then test these objects in isolation without
having to wire up a large object graph.</p>
]]></content>
  </entry>
  
</feed>
